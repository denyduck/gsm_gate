# Při startu aplikace se jako první importují signály a načtou se

#from django.apps import AppConfig

# signály pro dashboard aplikaci
#class DashboardConfig(AppConfig):
#    default_auto_field = 'django.db.models.BigAutoField'
#    name = 'dashboard'

    # při spuštění aplikace načíst signály
#    def ready(self):
#        import dashboard.signals  # Import signálů při spuštění aplikace